// Summary: This section gave me an understanding of creating a function with two arguments and calling the function to equal a certain number by using + in the () next to the function name.

// Example
function ourFunctionWithArgs(a, b) {
  console.log(a - b);
}
ourFunctionWithArgs(10, 5); // Outputs 5

// Only change code below this line.

function functionWithArgs(test1, test2) {
  console.log(test1 + test2);
}
functionWithArgs(1,2);
functionWithArgs(7,9);
